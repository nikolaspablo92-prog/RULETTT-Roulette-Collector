# RULETTT - Project Dependencies# RULETTT - Project Dependencies

# ===============================================# ===============================================

# Main requirements file that includes:# Main requirements file that includes:

# - requirements-core.txt (REQUIRED)# - requirements-core.txt (REQUIRED)

# - requirements-optional.txt (optional features)# - requirements-optional.txt (optional features)

##

# INSTALLATION OPTIONS:# INSTALLATION OPTIONS:

##

# 1. Minimal (core only):# 1. Minimal (core only):

#    pip install -r requirements-core.txt#    pip install -r requirements-core.txt

##

# 2. Full (core + optional):# 2. Full (core + optional):

#    pip install -r requirements.txt#    pip install -r requirements.txt

##

# 3. Development (core + optional + dev):# 3. Development (core + optional + dev):

#    pip install -r requirements.txt#    pip install -r requirements.txt

#    pip install -r requirements-dev.txt#    pip install -r requirements-dev.txt

# ===============================================# ===============================================



# Core dependencies (REQUIRED)# Core dependencies (REQUIRED)

-r requirements-core.txt-r requirements-core.txt



# Optional dependencies (for full functionality)# Optional dependencies (for full functionality)

-r requirements-optional.txt-r requirements-optional.txt


# Аутентификация и безопасность
pyjwt>=2.8.0           # JWT токены для авторизации
bcrypt>=4.1.0          # Хэширование паролей
cryptography>=41.0.0   # Криптография для ключей
python-dotenv>=1.0.0   # Переменные окружения (.env файлы)

# Планирование задач
apscheduler>=3.10.0    # Планировщик для ротации ключей и автозадач

# Кэширование сессий (опционально)
redis>=5.0.0           # Redis клиент для Python
hiredis>=2.2.0         # Быстрый парсер для Redis

# Дополнительные инструменты
pillow>=10.0.0         # Работа с изображениями (скриншоты)
python-magic>=0.4.27   # Определение типов файлов