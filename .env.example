# 🔐 RULETTT - Конфигурация безопасности
# ⚠️ НЕ ЗАГРУЖАЙТЕ ЭТОТ ФАЙЛ В ПУБЛИЧНЫЙ РЕПОЗИТОРИЙ!
# ⚠️ Добавьте .env в .gitignore

# =====================================================
# СЕКРЕТНЫЕ КЛЮЧИ (ОБЯЗАТЕЛЬНО ИЗМЕНИТЬ В ПРОДАКШЕНЕ!)
# =====================================================

# JWT секретный ключ (генерируйте свой уникальный)
SECRET_KEY=your-super-secret-key-change-this-in-production-123456789
JWT_SECRET=your-jwt-secret-key-change-this-too-987654321

# Соль для хэширования паролей
ADMIN_PASSWORD_SALT=random-salt-string-change-me-abcdef123456

# =====================================================
# БАЗА ДАННЫХ
# =====================================================

# Путь к основной базе данных
DATABASE_PATH=data/final_single_table.db

# Путь к базе аутентификации
AUTH_DATABASE_PATH=data/auth.db

# =====================================================
# НАСТРОЙКИ БОТА-СИМУЛЯТОРА
# =====================================================

# User-Agent для бота (измените на актуальный)
BOT_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36

# Разрешение экрана виртуального браузера
BOT_VIEWPORT_WIDTH=1920
BOT_VIEWPORT_HEIGHT=1080

# Headless режим (True = без GUI, False = с GUI для отладки)
BOT_HEADLESS=True

# Путь к Chrome/Chromium (оставить пустым для автоопределения)
CHROME_EXECUTABLE_PATH=

# Задержки бота (секунды)
BOT_MIN_DELAY=0.5
BOT_MAX_DELAY=3.5

# =====================================================
# PADDYPOWER АВТОМАТИЗАЦИЯ (НОВОЕ!)
# =====================================================

# ⚠️ ВНИМАНИЕ: Храните учетные данные в безопасности!
# ⚠️ НЕ ДЕЛИТЕСЬ ФАЙЛОМ .env С КРЕДИТАМИ!

# Учетные данные для автоматического входа в Paddypower
PADDYPOWER_USERNAME=ваш_email@example.com
PADDYPOWER_PASSWORD=ваш_пароль

# URL адреса Paddypower
PADDYPOWER_BASE_URL=https://www.paddypower.com
PADDYPOWER_LOGIN_URL=https://www.paddypower.com/login
PADDYPOWER_ROULETTE_URL=https://www.paddypower.com/casino/roulette

# Файл для сохранения cookies (сессии)
PADDYPOWER_COOKIES_FILE=data/paddypower_cookies.json

# Выходной файл для собранных данных
PADDYPOWER_OUTPUT_FILE=roulette_console_data.json

# =====================================================
# ВРЕМЕННЫЕ КЛЮЧИ ДОСТУПА
# =====================================================

# Рабочие часы для клиентов (через запятую, 0-23)
CLIENT_WORKING_HOURS=9,10,11,14,15,16,17,18,19,20

# TTL временных ключей (минуты)
KEY_TTL_MINUTES=120

# Максимум использований одного ключа
KEY_MAX_USAGE=1000

# Интервал автоматической ротации ключей (часы)
KEY_ROTATION_HOURS=2

# =====================================================
# REDIS (ОПЦИОНАЛЬНО - для кэширования сессий)
# =====================================================

# Включить Redis (True/False)
REDIS_ENABLED=False

# Хост и порт Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Пароль Redis (если требуется)
REDIS_PASSWORD=

# База данных Redis (0-15)
REDIS_DB=0

# TTL сессий в Redis (секунды)
REDIS_SESSION_TTL=28800

# =====================================================
# API НАСТРОЙКИ
# =====================================================

# Порт Flask API
API_PORT=5000

# Хост API (0.0.0.0 = доступен из сети, 127.0.0.1 = только локально)
API_HOST=0.0.0.0

# Режим отладки Flask
FLASK_DEBUG=True

# CORS разрешённые домены (через запятую)
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:5000

# =====================================================
# ЛОГИРОВАНИЕ
# =====================================================

# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Путь к файлу логов
LOG_FILE=logs/rulettt.log

# Максимальный размер файла лога (МБ)
LOG_MAX_SIZE_MB=10

# Количество бэкапов логов
LOG_BACKUP_COUNT=5

# Формат логов
LOG_FORMAT=[%(asctime)s] %(levelname)s in %(module)s: %(message)s

# =====================================================
# PUPPYPOWER COLLECTOR (интеграция с существующим)
# =====================================================

# XPath селектор истории рулетки
ROULETTE_XPATH=/html/body/div[2]/div/div[3]/div[1]/div/div[1]/div/div/div[2]/div[7]/div/div[3]/div

# Интервал автосбора (секунды)
COLLECTOR_INTERVAL=30

# Максимум сохранённых результатов
COLLECTOR_MAX_RESULTS=200

# =====================================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# =====================================================

# Режим разработки (True/False)
DEVELOPMENT_MODE=True

# Скрытый режим админов по умолчанию (True/False)
ADMIN_HIDDEN_MODE_DEFAULT=True

# Rate limiting (запросов в минуту)
RATE_LIMIT_PER_MINUTE=100

# Максимальное количество параллельных сессий одного клиента
MAX_CLIENT_SESSIONS=3

# Автоматическая очистка истёкших ключей (True/False)
AUTO_CLEANUP_EXPIRED_KEYS=True

# Интервал очистки (минуты)
CLEANUP_INTERVAL_MINUTES=15

# =====================================================
# GOOGLE CLOUD (опционально - для будущего масштабирования)
# =====================================================

# Включить Google Cloud интеграцию (True/False)
GOOGLE_CLOUD_ENABLED=False

# Путь к файлу credentials JSON
GOOGLE_APPLICATION_CREDENTIALS=

# Проект Google Cloud
GOOGLE_CLOUD_PROJECT=

# Bucket для хранения логов/данных
GOOGLE_CLOUD_STORAGE_BUCKET=
